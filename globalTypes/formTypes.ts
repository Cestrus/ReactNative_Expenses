export interface IFormState {
  date: {
    value: string;
    isValid: boolean;
  };
  amount: {
    value: string;
    isValid: boolean;
  };
  description: {
    value: string;
    isValid: boolean;
  };
}
